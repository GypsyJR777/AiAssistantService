quarkus:
  http:
    port: 8081

assistant:
  config:
    path: ${ASSISTANT_PATH}
  assistants:
    - BasicSearchAssistant

bot:
  telegram:
    token: ${TELEGRAM_TOKEN:}
    name: ${TELEGRAM_BOT_NAME:}